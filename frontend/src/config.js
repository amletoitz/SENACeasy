// ================================
// SENAC Easy — Configuração Global
// ================================
// Troque a URL abaixo pela URL do Railway depois do deploy.

const CONFIG = {
  // Em desenvolvimento: use o IP local da sua máquina (ex: http://192.168.1.100:5000)
  // Em produção: use a URL do Railway (ex: https://senaceasy.up.railway.app)
  API_URL: "http://192.168.1.100:5000",
};

export default CONFIG;
